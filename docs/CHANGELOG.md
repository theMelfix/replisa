# Changelog Replisa

Log cronologico (append-only) di tutto il lavoro completato. Una riga per task del backlog.

Formato: `YYYY-MM-DD · <task-code> · <commit-sha> · <descrizione breve>`

---

## Sprint 1 — Fondamenta (Settimane 1-2)

- 2026-05-14 · 1.1.1 · `ea35082` · Scaffold Laravel app (MySQL + Pest, no starter kit auth)
- 2026-05-14 · 1.1.2 · `ea35082` · Git repo `theMelfix/notifly` (privato), branch `main`/`develop`/`master`, `.gitignore` Laravel + esclusione `.claude/settings.local.json`
- 2026-05-14 · 1.1.3 · `ea35082` · `.env`/`.env.example` con placeholder Meta WhatsApp Cloud API (`META_GRAPH_API_VERSION`, `META_WHATSAPP_PHONE_NUMBER_ID`, `_BUSINESS_ACCOUNT_ID`, `_ACCESS_TOKEN`, `_APP_SECRET`, `_WEBHOOK_VERIFY_TOKEN`)
- 2026-05-14 · ADR-003 · `7c951a7` · Multi-tenant credential model: una Meta App Notifly (Tech Provider), credenziali app-level in `.env`, credenziali per-tenant in tabella `tenants`. `.env.example` ristrutturato. Aggiunto ICE-11 (Embedded Signup) all'Icebox
- 2026-05-15 · 1.2.1 · — · Meta Business App `Notifly` creata (app type Business, use case "Manage messaging on WhatsApp Business Platform")
- 2026-05-15 · 1.2.2 · — · Prodotto WhatsApp attivato; Test WABA `1333683675528873` + numero sandbox creati
- 2026-05-15 · 1.2.3 · — · Raccolti `PHONE_NUMBER_ID=1147055808484414` e `WABA_ID=1333683675528873`. Numero personale +39 338 485 2605 verificato come tester. Bump `META_GRAPH_API_VERSION=v25.0` (era v21.0, in scadenza)
- 2026-05-15 · 1.2.4 · — · System User Token permanente generato (scope `whatsapp_business_messaging` + `_management`, accesso solo Test WABA). Salvato in `.env` come `META_WHATSAPP_SANDBOX_ACCESS_TOKEN`
- 2026-05-15 · sec · — · App Secret resettato (era stato esposto in chat). Grace period 0h
- 2026-05-15 · 1.2.5 · — · Primo invio template `hello_world` riuscito (HTTP 200, message_status=accepted). Script bash riusabile in `scripts/whatsapp-test-send.sh`
- 2026-05-15 · ADR-004 · — · Rename prodotto **Notifly → Replisa** (notifly.it/.com/.app non disponibili a prezzo standard; 18 alternative valutate). Acquistati `replisa.com` (primario) + `replisa.it` (difensivo 301→.com). Backlog rinominato in `replisa_backlog.md`. Repo GitHub e Meta App da rinominare manualmente
- 2026-05-15 · infra · — · Cartella locale rinominata `/home/gmelfi/notifly/` → `/home/gmelfi/replisa/`
- 2026-05-15 · 1.1.5 · — · VPS IONOS con CloudPanel: site PHP `replisa.com` (PHP 8.4, Laravel template). DNS IONOS: A `@`/`www`/`app`/`api` → VPS IP. SSL Let's Encrypt attivo su `replisa.com` + `www`. Site secondario `replisa.it` come Static Site con redirect 301 NGINX → `replisa.com` + SSL incluso
- 2026-05-15 · 1.2.6 · — · Bitwarden cloud (free tier) attivato. Credenziali Meta App, WABA, System User Token, Webhook Verify Token salvate
- 2026-05-15 · 1.1.4 · — · MariaDB su VPS IONOS. DB `replisa` (charset utf8mb4) + user `replisa_app` con privilegi solo sul DB, bind localhost. Creati via CloudPanel UI. Credenziali in Bitwarden
- 2026-05-16 · 1.1.6 · — · Deploy pipeline DPLOY (CloudPanel) configurata. Release atomiche Capistrano-style su `/home/replisa-com/htdocs/replisa.com/{releases,shared,current}`. Shared: `storage/app` + `storage/logs`. Overlays: `.env` produzione (APP_KEY generato con `openssl`). Sudoers: `replisa-com` può `systemctl reload php8.4-fpm` senza password. Vhost NGINX CloudPanel: `{{root}}` sostituito con `current/public` (HTTP 443 + 8080 internal). Primo deploy `main`: **https://replisa.com serve Laravel 13 default page** 🎉 Sprint 1 chiuso definitivamente

## Sprint 2 — Motore & Comunicazione (Settimane 3-4)

- 2026-06-08 · 2.3.1 · — · Migration + model `Tenant`: `name`, `phone_number_id` (unique), `waba_id`, `access_token` (cast `encrypted`, `hidden` — ADR-003), `plan`, `active`. Relazioni `contacts()`/`messages()`. Verificato cast encrypted a riposo via tinker
- 2026-06-08 · 2.3.2 · — · Migration + model `Contact`: FK `tenant_id` (cascade), `phone`, `name`, `opted_in`+`opted_in_at` (GDPR), `last_seen_at`. Unique `(tenant_id, phone)` — numero unico per tenant, non globale
- 2026-06-08 · 2.3.3 · — · Migration + model `Message`: FK `tenant_id`+`contact_id` (cascade), `direction` (outbound/inbound), `type`, `content` (json), `status`, `meta_message_id` (index per match status webhook), `error` (json)
- 2026-06-08 · 2.1.1-2.1.6 · — · `WhatsAppService` (E2.1 completo): wrapper Meta Cloud API scoped per-tenant (`::for($tenant)`, ADR-003). `sendTemplate()` (componenti dinamici), `sendText()`, `sendButtons()` (max 3) e `sendList()` (max 10) con validazione. Retry+backoff esponenziale solo su errori transitori (429/5xx/connessione) via `Http::retry`; `WhatsAppApiException` con `metaCode`/`httpStatus` sui permanenti. Ogni invio loggato su `messages` (queued→sent+meta_message_id / failed+error). Config `services.meta.*`. Coperto da 7 test Pest con `Http::fake` (payload, logging, retry, errori, validazione)
- 2026-06-09 · infra · `198b5b8` · Queue worker infra-as-code: `deploy/replisa-worker.service` (systemd, `queue:work` coda database) + `deploy/README.md` (setup worker, restart post-deploy via sudoers, checklist deploy engine, nota scheduler E3.2). `main` allineato a Sprint 2 (merge `6cd69d4`); push + esecuzione deploy/worker/Meta-webhook sul VPS a carico del dev (vedi runbook)
- 2026-06-09 · 2.3.4-2.3.6 · — · Schema P1 completato (E2.3 chiuso). `conversations` (finestre billing 24h: category, billable, opened_at/expires_at), `automations` (flussi per-tenant: type+trigger+config json, unique tenant+type, costanti TYPE_*), `appointments` (scheduled_at, status, reminded_at, review_requested, costanti STATUS_*). Relazioni inverse aggiunte a `Tenant`/`Contact`. Cast e cascade delete verificati via tinker
- 2026-06-09 · 2.2.1-2.2.6 · — · Webhook Meta (E2.2 completo). `GET /webhook` verifica challenge (`hash_equals` sul verify token); `POST /webhook` riceve eventi. Route in `routes/webhook.php` registrate fuori dal gruppo `web` (no CSRF/sessione) via `then:` in bootstrap. Firma `X-Hub-Signature-256` validata dal middleware `meta.signature` (HMAC-SHA256 del body raw con app_secret, fail-closed). Processing async via job `ProcessWhatsAppWebhook` (idempotente su `meta_message_id`): parsing messaggi inbound (text/interactive/button) con upsert contatto + `last_seen_at`, e status update (delivered/read/failed → colonna `status`/`error`). Endpoint risponde `EVENT_RECEIVED` (200) subito. Coperto da 11 test Pest. ⚠️ NB: in produzione serve un queue worker attivo (`queue:work`, connection `database`) — da aggiungere al deploy DPLOY

## Sprint 3 — Flussi & Dashboard (Settimane 5-6)

- 2026-06-09 · 3.1.2-3.1.5 · `2e859a0` · Welcome Flow (E3.1, lato codice). Nuovo `App\Services\Automation\WelcomeFlow` (scoped per-tenant come `WhatsAppService`), agganciato a `ProcessWhatsAppWebhook`. Trigger primo contatto su `$contact->wasRecentlyCreated`, eseguito solo sui messaggi appena loggati (no doppio invio su webhook riconsegnato). `greet()` invia il menu interattivo via `sendButtons` (free-form nella finestra 24h → nessun template Meta richiesto); testo, header/footer e bottoni personalizzabili per-tenant via `automations.config`, con default Info Servizi/Prenota/Parla con noi. Id bottone namespaced `welcome:<action>`; `handleButtonReply()` instrada al testo in `config.replies[action]`. Opt-in registrato al primo contatto (`opted_in`+`opted_in_at`, idempotente). Il flusso parte solo se esiste un'`Automation` `welcome` attiva per il tenant. Coperto da 7 test Pest (Http::fake). Template Meta 3.1.1 ancora da sottomettere (non blocca)

- 2026-06-17 · 3.3.2/3.3.4 · `f9e710d` · Richiesta Recensione (E3.3, lato codice). Nuovo `App\Services\Automation\ReviewRequest` (scoped per-tenant) + command `replisa:send-review-requests` schedulato `->hourly()->withoutOverlapping()` (stesso cron `schedule:run` del reminder, nessuna infra nuova). `dispatchDue()` trova gli appuntamenti `completed` con `review_requested=false` e `scheduled_at <= now-delay` (default 24h, `config.delay_hours`) e invia il **template** MARKETING `review_request` (obbligatorio: serve opt-in del contatto, altrimenti l'appuntamento è saltato ma marcato `review_requested` per non rivalutarlo). Body param nome contatto; button URL dinamico opzionale via `config.review_url_param`. Rate limit per-contatto (3.3.4): `recentlyRequested()` blocca una nuova richiesta se un template recensione non-`failed` è già partito entro `config.rate_limit_days` (default 30g), interrogando la tabella `messages` su `content->template`. Fallimenti API loggati senza marcare `review_requested` → retry al giro dopo. Coperto da 10 test Pest (Http::fake). Template Meta 3.3.1 ancora da sottomettere (non blocca il codice); 3.3.3 (tracking click) differito — i click sul button URL non passano dal webhook
- 2026-06-10 · 3.2.2-3.2.4 · `e45e73b` · Reminder Appuntamento (E3.2, lato codice). Nuovo `App\Services\Automation\AppointmentReminder` (scoped per-tenant) + command `replisa:send-reminders` schedulato `->hourly()->withoutOverlapping()` in `routes/console.php`. `dispatchDue()` trova gli appuntamenti `scheduled` con un reminder dovuto e li invia: due finestre (-24h / -2h, configurabili via `automations.config.offsets`) gestite in modo idempotente sull'unica colonna `reminded_at` calcolando l'offset "dovuto" più urgente non ancora coperto. `remind()` invia il **template** `appointment_reminder` (obbligatorio fuori finestra 24h) con body params nome/data/ora; i fallimenti API sono loggati senza marcare `reminded_at` (retry al giro dopo). Risposte ai quick-reply del template (messaggi inbound tipo `button`) instradate da `handleButtonReply()`: payload `CONFIRM`→`confirmed`, `CANCEL`→`cancelled` sul prossimo appuntamento scheduled del contatto; agganciato a `ProcessWhatsAppWebhook`. Coperto da 10 test Pest. ⚠️ Prod: serve cron `schedule:run` (deploy/README aggiornato) e il template Meta 3.2.1 approvato. 3.2.5 (endpoint API appuntamenti) differito a E4.3 (dipende da Sanctum)
- 2026-06-17 · legal · `7657750`/`93a02be`/`0888f38` · Pagine legali richieste da Meta, pubblicate su replisa.com: `/privacy` (informativa GDPR col doppio ruolo titolare/responsabile), `/termini` (condizioni d'uso), `/eliminazione-dati` (istruzioni cancellazione dati). Route `web.php` + layout dedicato. Dati: Giovanni Melfi, libero professionista, P.IVA IT01809180886, sede Vittoria (RG), contatto info@giovannimelfi.it. ⚠️ Bozze da far revisionare a un legale prima dei clienti paganti (E6.2.2)
- 2026-06-17 · brand · `c944caf` · Icona app 1024×1024 richiesta da Meta (`brand/replisa-icon-1024.png`, verde brand + "R" bianca) con script di generazione `brand/generate-icon.php` (PHP GD)
- 2026-06-17 · infra · `a086490` · Command `replisa:seed-sandbox-tenant`: crea/aggiorna un tenant di test dalle credenziali sandbox del `.env` (ADR-003: invio/ricezione scoped per-tenant via DB, non dal `.env`). 3 test Pest
- 2026-06-17 · sec · — · Token Meta in `.env` era un token **temporaneo scaduto** (15-May), non il System User permanente → invio fallito (err 190). Rigenerato System User token (expiration Never, scope messaging+management) e propagato all'overlay DPLOY persistente. Esposto inavvertitamente in chat (selezione IDE) → rigenerato di nuovo. `.env` di prod vive in `~/.dploy/overlays/.env`; config cachata richiede `config:clear`
- 2026-06-17 · release · `1e009e5` · **Primo rilascio applicativo di replisa.com** (Sprint 2 + Sprint 3). Merge `develop`→`main`, deploy DPLOY, attivazione infra VPS: queue worker systemd, cron `schedule:run` (reminder + recensioni), webhook Meta verificato e sottoscritto al campo `messages`. Test end-to-end live OK: invio via app → log `messages` → webhook firmato → worker → status `sent`→`delivered`→`read`. Engine WhatsApp + Welcome Flow operativi in produzione (reminder/recensioni attendono i template Meta approvati)
- 2026-06-19 · 4.2.4 · `b6843c5` · **Dashboard — Appuntamenti** (E4.2). Livewire `Appointments` (`/appointments`): form nuovo appuntamento (crea il contatto via `firstOrCreate`), cambio stato (programmato/confermato/disdetto/completato), eliminazione, import CSV (`telefono,nome,data`, parser robusto multi-formato). Scoped per-tenant. Link in navbar. 6 test, 105 totali
- 2026-06-19 · 4.2.2/4.2.5 · `5a28377` · **Dashboard — Contatti e Log messaggi** (E4.2). Livewire `Contacts` (`/contacts`): lista paginata + ricerca nome/telefono + conteggio messaggi. Livewire `Messages` (`/messages`): cronologia paginata con badge stato, ricerca per contatto e filtri direzione/stato. Gli OR di ricerca sono raggruppati in closure per non rompere l'isolamento del TenantScope. Link in navbar. 6 test (isolamento + ricerca/filtri), 99 totali. Restano: dettaglio conversazione (4.2.2), filtri data/tipo (4.2.5)
- 2026-06-19 · 4.2.1/4.2.3 · `e34885a` · **Dashboard cliente — overview + automazioni** (E4.2, parziale). Livewire `Dashboard`: card messaggi inviati/ricevuti, contatti, conversazioni attive, appuntamenti in arrivo (scoped per-tenant via TenantScope). Livewire `Automations` (`/automations`): toggle on/off dei 3 flussi (welcome/reminder/review) che crea/aggiorna la riga `Automation` del tenant. Route dashboard convertita a componente Livewire, link in navbar. 8 test (93 totali). Restano: config parametri flussi (4.2.3), grafici/costi (4.2.1), e sezioni Contatti/Appuntamenti/Log (4.2.2/4.2.4/4.2.5)
- 2026-06-19 · 5.1.1 · `e9be832` · **Landing page replisa.com** (v1): sostituisce la welcome di default Laravel. Hero, sezione 3 automazioni, prezzi (4 piani Starter/Base/Pro/Business), CTA demo (`mailto`), footer con link legali. Login/registrazione condizionati alle rotte Breeze. Tailwind via Vite → richiede `npm run build` in deploy (vedi deploy/README, opzione A: build hook DPLOY). SEO base (title/description/OG) inclusa; structured data JSON-LD ancora da fare (5.1.3)
- 2026-06-17 · 4.1.1-4.1.4 · `56dc28b`→`0986537` · **E4.1 Auth & Multi-Tenancy** (su `develop`). Breeze stack Livewire/Volt (login/registrazione/reset). Registrazione self-service: crea `Tenant` + utente `owner` in transazione (campo "Nome attività"); `users.tenant_id` nullable (super-admin=null). Ruoli via `spatie/laravel-permission` (`super-admin`/`owner`/`operator`, RoleSeeder). Isolamento dati: `TenantScope` globale + trait `BelongsToTenant` sui 5 modelli tenant-owned, no-op in console/webhook e per super-admin, con auto-fill `tenant_id`. Area super-admin `/admin/tenants` (Livewire `Admin\Tenants`, toggle attivo) protetta da `role:super-admin`; command `replisa:create-admin`. Dashboard su `app.replisa.com` (da configurare in deploy). 38 nuovi test (85 totali verdi). ⚠️ Non ancora rilasciato in prod
- 2026-06-19 · fix · — · **Log di produzione interrotti** (`open stream: No such file or directory`). DPLOY crea i symlink `storage/{app,logs}` verso la shared ma **non popolava le directory target** → symlink penzolanti, ogni scrittura del log falliva. Causa scatenante: un super-admin (tenant_id null) che toccava un toggle in Automazioni generava un insert senza `tenant_id` → MySQL `1364` ripetuto, e il logging dell'errore falliva a sua volta. Fix infra: create `shared/storage/{app/public,logs}` (owner `replisa-com`) + aggiunto `mkdir -p` difensivo come primo `before_commands` DPLOY perché la shared sia sempre inizializzata. NB: `storage/framework` resta per-release (session/cache/queue su `database`, nessun logout a deploy)
- 2026-06-19 · 4.2.3 · `856b7fa`→`38e182a` · **Backstop tenant + toast in-app per le eccezioni** (hardening E4.2.3/E4.1.2). Backstop in `BelongsToTenant`: un utente autenticato senza tenant (es. super-admin) che tenta una scrittura per-tenant riceve una `TenantContextException` con messaggio chiaro invece del `1364` criptico che inquinava i log (contesti senza Auth restano liberi). Nuovo sistema di notifiche effimere `<x-toast-hub />` (Alpine, in alto a destra, auto-dismiss): rete di sicurezza client (hook Livewire `request` → ogni 500 mostra un toast generico, il messaggio reale resta solo nei log del server, non esposto in prod) + errori di dominio attesi catturati lato server con toast specifico (`Automations::toggle()` → `dispatch('toast', ...)`). Verificato live in prod. 106 test verdi
- 2026-06-19 · brand · `d28b6f1` · **Favicon e web manifest Replisa**. Pacchetto favicon completo in `public/` (ico, png 16/32, apple-touch-icon, android-chrome 192/512) con i `<link>` nei 4 layout (`app`, `guest`, `legal`, `welcome`). `site.webmanifest` valorizzato: `name`/`short_name` "Replisa", `theme_color` brand `#16a34a`
- 2026-06-19 · i18n · `2a5588b` · **Logo Replisa + UI in italiano**. `application-logo` ora usa l'icona brand (`public/images/logo.png`) al posto del logo Laravel di default (navbar + pagine guest). i18n: `APP_LOCALE=it` (default config `it`, fallback `en`), `lang/it.json` (label Breeze: login/registrazione/profilo/nav) + file framework `lang/it/{auth,passwords,validation,pagination}.php` → validazione e messaggi auth in italiano. ⚠️ Prod: impostare `APP_LOCALE=it` nell'overlay DPLOY + `config:clear`
- 2026-06-20 · 4.2.6 · `dbe06ae` · **Integrazione Stripe (Laravel Cashier)** + pagina Abbonamento. Cashier `^16.5` con `Billable` sul modello **Tenant** (è l'attività a sottoscrivere, non l'utente): migrazioni adattate a `tenant_id`/tabella `tenants`, `Cashier::useCustomerModel(Tenant)`. `config/plans.php`: i 4 piani (Starter/Base/Pro/Business) → Price ID Stripe da `.env` + limiti contatti/automazioni (futuro enforcement). Livewire `Billing` (`/billing`, link nel menu utente, nascosto ai super-admin): griglia piani → **Stripe Checkout** ospitato, gestione/disdetta via **Billing Portal**; errori come toast. Webhook `POST /stripe/webhook` auto-registrato. Chiavi `STRIPE_*`/`CASHIER_*` in `.env.example`. 4 test (110 totali). ⚠️ Richiede setup lato Stripe (prodotti/prezzi, API keys, webhook) negli overlay prod
- 2026-06-20 · 6.2 · `3e62873` · **Form collegamento account WhatsApp** (onboarding interim). Livewire `WhatsAppSettings` (`/whatsapp`, "Account WhatsApp" nel menu utente): il tenant collega il numero inserendo `phone_number_id`, `waba_id`, `access_token` della Meta Cloud API; token salvato criptato (ADR-003) e mai ri-esposto (campo vuoto = invariato). Pulsante "Verifica connessione" che interroga in live la Graph API di Meta. Interim verso l'Embedded Signup self-service (ICE-11). 7 test (117 totali)
- 2026-06-20 · 4.2.6 · `597121b` · **Conteggio messaggi/contatti nella sezione Billing**. Riquadro "Utilizzo" in `/billing`: messaggi inviati e ricevuti nel mese corrente (scoped per-tenant) e contatti totali vs limite del piano attivo (∞ se illimitato, evidenziato se oltre soglia). Base per il futuro enforcement dei limiti. 1 test (118 totali)
- 2026-06-20 · 4.2.6 · `b31fc10` · **Enforcement dei limiti di piano**. Nuovo `App\Support\PlanLimits`: risolve il piano del tenant dal prezzo dell'abbonamento Stripe (default `starter` se non abbonato, `config('plans.default')`) ed espone i limiti contatti/automazioni. Blocchi nei punti di creazione: `Automations::toggle()` (attivazione oltre il numero consentito → toast; Starter = 1), form Appuntamenti + import CSV (contatti **nuovi** oltre il limite bloccati, gli esistenti restano usabili, l'import riporta i saltati). La card "Utilizzo" del Billing usa il limite effettivo da `PlanLimits`. NB: il webhook inbound non è soggetto a enforcement (non si scartano messaggi reali). 2 test (120 totali)
- 2026-06-21 · prodotto · `8f42972` · **Revisione prodotti e prezzi**. Mix prodotti: reminder appuntamenti → **"Promemoria & Scadenze"** (appuntamenti + scadenze tipo IMU/730, con o senza conferma); nuovo flusso core **"Campagne e comunicazioni"** (`TYPE_CAMPAIGN`, invii a liste/segmenti) al posto di **Richiesta recensione**, che diventa **add-on**. Aggiornati i 3 flussi in UI Automazioni e le card della landing. Prezzi: **29/69/129/249** €/mese (erano 14/39/79/149); nuovo add-on **Recensioni Google €19/mese** (incluso in Business) in `config/plans.php`. ⚠️ Solo pacchettizzazione/marketing: gli engine di invio Campagne e di Promemoria generici, e il gating dell'add-on Recensioni, restano da costruire
- 2026-06-21 · 6.2.3 · `d0f9b38` · **Registrazione con dati fiscali + verifica P.IVA** (anti-spam). Form di registrazione esteso con i dati fiscali del tenant (ragione sociale, P.IVA, CF, indirizzo, CAP, provincia, Codice SDI **o** PEC) → nuove colonne su `tenants`, P.IVA unica. Verifica P.IVA su due livelli: `App\Support\ItalianVat` (checksum 11 cifre, Luhn con raddoppio posizioni pari) via regola `App\Rules\ItalianVatChecksum` + `App\Services\Vies\ViesClient` (verifica live esistenza/attività su VIES UE). Policy: P.IVA esplicitamente inesistente → blocca; VIES irraggiungibile → procede con `vat_validated_at` nullo (verifica manuale lato admin). 14 test (129 totali)
- 2026-06-21 · 4.1.4/E5 · `60ea74a` · **Area admin clienti + licenze offline + blocco**. `/admin/tenants` potenziato: per ogni tenant dati fiscali (P.IVA + badge validata VIES), **piano effettivo** con fonte (Offline/Stripe/Free) e **MRR** stimato. Azioni: blocca/sblocca, **disdici** (Cashier `cancelNow`), **rimborsa** ultimo pagamento (Cashier `refund`) — guardate con toast. **Licenze offline (E5)**: colonna dedicata `manual_plan` (+ scadenza), assegnabile/revocabile da admin; `PlanLimits` dà priorità alla licenza offline su Stripe (poi Stripe, poi default). **Blocco**: middleware `tenant.active` → i tenant disattivati sono rediretti a `/suspended` (super-admin liberi). 10 test (137 totali)
- 2026-06-21 · E5 · `39baad3` · **Censimento cliente da admin + UI licenza**. Pulsante "Nuovo cliente" in `/admin/tenants`: crea tenant + utente **owner** (email/password d'accesso), P.IVA opzionale (solo checksum, no VIES — admin fidato) e licenza offline opzionale; pannello collassabile (Alpine). UI: select piano + data licenza spostate in una **colonna "Licenza offline" dedicata**, separate dai bottoni azione. 2 test (139 totali)
- 2026-06-21 · E5 · `2d528b7` · **Invito via email al censimento** (niente password in chiaro). Creando un cliente da admin non si imposta più la password: l'owner è creato con password casuale e riceve `TenantInvitation` (email con **URL firmato**, valido 7 giorni). La pagina `AcceptInvitation` (`/invito/{user}`, middleware `signed`) fa impostare la password e accede; invito **monouso** (`email_verified_at`). ⚠️ Prod: serve `MAIL_*` SMTP configurato nell'overlay (in locale `MAIL_MAILER=log`). 5 test (143 totali)
- 2026-06-21 · pricing · — · **Analisi concorrenza + piano revisione prezzi** (`docs/PRICING-REVIEW.md`). Benchmark Spoki (€19/39/55), Callbell (per-agente + broadcast €0,02/contatto), Wati (~€55/110/279 + markup 20%), gestionali verticali (€7-12). Insight: proposta 29/69/129/249 allineata all'orizzontale; mancano tier free e monetizzazione campagne. Decisioni da prendere prima di ritoccare i prezzi. Nessuna modifica prezzi applicata
- 2026-06-21 · 3.4.2 · `c630ecb` · **Engine Campagne** — invio template a segmento opt-in. Modello `Campaign` + migrazione (per-tenant, stato + conteggi). Livewire `Campagne` (`/campaigns`, link in navbar): compositore (nome, template Meta, lingua, personalizzazione nome) con conteggio destinatari opt-in e storico; l'invio crea la campagna e accoda `SendCampaign`. Job `SendCampaign` (`ShouldQueue`): manda il template MARKETING a tutti i contatti **opted-in** del tenant via `WhatsAppService` (log per messaggio), aggiorna conteggi/stato. ⚠️ Template Meta da approvare a parte; gating per piano e segmentazione avanzata (tag/filtri, 3.4.3) sono follow-up. 5 test (148 totali)
- 2026-06-21 · 3.2.6 · `d498d56` · **Engine Promemoria & Scadenze** (nazionali + proprie). Modelli `Deadline` (`tenant_id` null = nazionale gestita admin, valorizzato = propria del tenant) e `DeadlineReminder` (config per-tenant: template + giorni di anticipo). Seeder `NationalDeadlinesSeeder` con date fiscali **2026** (IMU acconto/saldo, 730, Redditi PF, versamenti — da verificare a ogni annuncio). Admin `/admin/deadlines`: CRUD calendario nazionale. Tenant `/scadenze`: nazionali + proprie, attiva promemoria e aggiunge scadenze. Command `replisa:send-deadline-reminders` (schedulato ogni ora): allo scattare della finestra avvia una campagna verso gli opted-in (riusa `SendCampaign`), `dispatched_at` evita reinvii. 13 test (157 totali)
- 2026-06-21 · 3.2.6 · `005681a` · **Settori** — scadenze nazionali filtrate per categoria attività. Le nazionali non valgono per tutti: hanno un `sector` (es. fiscali IMU/730 = `commercialista`); il tenant ha un proprio `sector` e vede solo le nazionali del suo settore + le universali (sector null) + le proprie (un dentista non vede IMU/730). Colonne `sector` su `tenants`/`deadlines`, `config/sectors.php`, `Deadline::visibleTo($tenantId, $sector)`. Settore **richiesto** in registrazione e nella creazione cliente admin; selezionabile per scadenza in `/admin/deadlines`. 1 test (158 totali)
- 2026-06-21 · 3.2.6 · `(docs)` · **Modifica settore tenant da admin**: select inline per riga in `/admin/tenants` per assegnare/cambiare il settore (utile per i tenant esistenti con `sector` null). 1 test (159 totali)
- 2026-06-22 · 4.2.6 · `dcac5e6` · **Gating per piano + add-on Recensioni**. Layer entitlement su `PlanLimits`: `allows($feature)` — Campagne e Promemoria scadenze inclusi dal piano **Base** in su (Starter no), gate in `Campaigns::send()`/`Deadlines::saveReminder()` con banner upsell + difesa nei command. `hasReviewsAddon()` — la Richiesta recensione richiede l'add-on (incluso nel **Business** o concesso via `tenants.reviews_addon`): gate nel command `send-review-requests`; in Automazioni la card Recensioni è un add-on attivabile solo se entitled; admin può attivare l'add-on per tenant. `config/plans.php`: `features` per piano. 6 test (164 totali)
- 2026-06-23 · pricing · `6d0b6ca` · **Nuovi prezzi + trial + annuale + pacchetti campagne** (decisioni `PRICING-REVIEW.md`). Prezzi **19/49/99/199** €/mese (config + landing). **Prova 14 giorni** senza carta: `trial_ends_at` alla registrazione, `PlanLimits` dà il piano `trial_plan` (Pro) durante la prova (`planSource` = 'trial'). **Annuale -20%**: `price_annual` + `STRIPE_PRICE_*_ANNUAL`, toggle mensile/annuale su landing e `/billing` (subscribe usa il prezzo giusto). **Pacchetti messaggi campagna**/mese per piano (`limits.campaign_messages`): enforcement in `Campaigns::send()` + card "Messaggi campagna" nell'utilizzo Billing. ⚠️ Overage a consumo (metered) e creazione Price ID Stripe (mensili+annuali) = follow-up. 7 test (166 totali)
- 2026-06-29 · 3.2.1/3.3.1 · `(docs)` · **Specifica template WhatsApp** (`docs/META-TEMPLATES.md`). Definizione canonica dei message template Meta allineata 1:1 col codice: `appointment_reminder` (UTILITY, body {{1}}nome/{{2}}data/{{3}}ora + 2 quick-reply), `review_request` (MARKETING, body {{1}}nome + button URL statico/dinamico), template di esempio per Campagne e Promemoria scadenze, nota Welcome Flow (free-form, niente template). Chiarito che i template vivono **per-WABA** (ADR-003) → vanno replicati in ogni WABA tenant; procedura di submission + checklist nomi attesi dal codice. Due action item lato codice annotati: allineare i payload quick-reply (`CONFIRM`/`CANCEL`) ai testi italiani e gestire il param del button recensione. Task 3.2.1/3.3.1 → 🟡 (spec pronta, resta l'approvazione manuale su WhatsApp Manager)
- 2026-06-29 · 3.2.4 · `(code)` · **Payload quick-reply reminder allineati alle label italiane**. `AppointmentReminder`: default `DEFAULT_CONFIRM_PAYLOAD`/`DEFAULT_CANCEL_PAYLOAD` portati da `CONFIRM`/`CANCEL` a **`Confermo`/`Disdico`** (coincidono col testo dei bottoni quick-reply del template `appointment_reminder`, che Meta restituisce come `button.payload`). `handleButtonReply()` ora normalizza il match **case-insensitive** e mantiene `confirm`/`cancel` come fallback di retrocompatibilità. Un template con bottoni `Confermo`/`Disdico` aggiorna lo stato appuntamento senza configurazione extra. 2 nuovi test (168 totali). Action item §1 di META-TEMPLATES.md chiuso
- 2026-06-29 · 3.3.1/4.2.6 · `(code)` · **Configurazione link recensione da UI (guardrail statico/dinamico)**. Pannello inline nella card *Richiesta recensione* di `/automations` (visibile quando l'add-on è attivo): il tenant sceglie se il bottone URL del template è **statico** (link già nel template → `Automation.config.review_url_param` null) o **dinamico** (suffisso obbligatorio passato al `{{1}}` del bottone). `Automations::saveReviewSettings()` valida (statico→null, dinamico→required no-spazi, niente regola `url` perché è un suffisso, non un URL completo), fa merge della config (preserva altre chiavi) ed è gated sull'add-on (`hasReviewsAddon`). La modalità si inferisce al `mount` dal param esistente. Chiude gli stati incoerenti del button URL che fallivano a runtime (param di troppo / param mancante); residuo non verificabile lato app (modalità vs template reale Meta) = follow-up opzione 3. Action item §2 di META-TEMPLATES.md chiuso. 6 nuovi test (174 totali)
- 2026-07-20 · 4.2.3 · `(code)` · **Configurazione parametri dei flussi da UI** (chiude E4.2.3). Ogni flusso attivo espone un pannello inline in `/automations`. *Benvenuto*: messaggio di saluto, header/footer facoltativi e fino a 3 bottoni con la risposta automatica associata (`Automations::saveWelcomeSettings()`); gli **id dei bottoni non sono editabili** — restano stabili tra i salvataggi perché sono la chiave con cui il webhook instrada il reply (uno slot lasciato senza titolo viene scartato, ma serve almeno un bottone: Meta ne richiede 1-3). *Promemoria*: nome del template Meta (validato sul formato `[a-z0-9_]`), lingua, anticipi in ore (lista tipo "24, 2" → normalizzata, deduplicata e ordinata dalla più lontana) e testo dei bottoni conferma/disdetta, che deve coincidere con le label del template (vedi `META-TEMPLATES.md §1`). Un anticipo non plausibile è un **errore di validazione**, non uno scarto silenzioso. *Campagne*: rimando alla sezione dedicata. I default mostrati in UI vengono ora dalle costanti pubbliche `WelcomeFlow::DEFAULT_GREETING`/`DEFAULT_BUTTONS` e `AppointmentReminder::DEFAULT_*` (unica sorgente per UI ed engine). Scrittura config unificata in `updateConfig()` (merge non distruttivo + gestione `TenantContextException`), usata anche da `saveReviewSettings()`. 14 nuovi test, incluso uno end-to-end che verifica che il menu salvato dalla UI sia esattamente quello inviato a Meta e che la risposta al bottone risolva la reply salvata (188 totali)
- 2026-07-20 · 4.3.1-4.3.5/3.2.5 · `(code)` · **API pubblica v1** (E4.3 completa, chiude anche 3.2.5). Laravel Sanctum installato; token personali agganciati al modello **User** — non al Tenant — così l'isolamento dati continua a passare per il `TenantScope` già testato (che legge `user()->tenant_id`), senza riscrivere il cuore del multi-tenancy. Rotte in `routes/api.php` sotto `auth:sanctum` + `tenant.active` + `throttle:api` (60 req/min per token, limiter registrato in `AppServiceProvider`), prefisso `/api/v1`. **Endpoint**: `POST /messages/send` (`MessageController@send`, tipo text o template con params posizionali; il contatto è risolto nel controller per non aggirare il limite contatti del piano; errore Meta → 502 con `meta_code`), `POST /appointments` (`AppointmentController@store`, crea appuntamento + contatto nei limiti del piano, `scheduled_at` deve essere futuro, il reminder parte poi dallo scheduler E3.2), `GET /messages` (`MessageController@index`, paginato + filtri direction/status/per_page, `MessageResource`). `EnsureTenantActive` ora risponde 403 JSON alle richieste API (invece del redirect HTML a `/suspended`). **UI**: `ApiTokens` (`/api-tokens`, "API & Integrazioni" nel menu utente) genera/revoca le chiavi mostrando il token in chiaro una sola volta; pagina `/api-docs` con guida in italiano ed esempi cURL (task 4.3.5, no Swagger — target PMI con un dev). Estratto `Contact::normalizePhone()` (prima duplicato in 3 punti) come unica sorgente di normalizzazione E.164. 22 nuovi test (210 totali). ⚠️ Prod: nessuna infra nuova (Sanctum usa la tabella `personal_access_tokens`, migrazione inclusa)
- 2026-07-21 · 5.1.2 · `(code)` · **Form contatto/demo sulla landing** (E5.1.2). Sostituito il CTA `mailto:info@giovannimelfi.it` con un vero form: Livewire `ContactForm` nella nuova sezione `#demo` di `welcome.blade.php` (link "Demo" in navbar). Campi nome/email (obbligatori) + telefono/attività/messaggio; all'invio salva un `Lead` (nome, email, phone, business, message, status `new`, ip) e notifica il team via email **on-demand** (`Notification::route('mail', config('services.contact.notify_email'))` → `NewLeadNotification`, con `replyTo` sul lead). `Lead` **non è tenant-owned** (arriva da un visitatore anonimo): niente BelongsToTenant/TenantScope, migrazione `leads` senza `tenant_id`. Anti-spam: honeypot (campo `website` nascosto) che finge il successo senza salvare né notificare. Config `services.contact.notify_email` (default `info@giovannimelfi.it`, override via `CONTACT_NOTIFY_EMAIL`). 6 test (216 totali). ⚠️ Prod: serve `MAIL_*` SMTP + `CONTACT_NOTIFY_EMAIL` nell'overlay perché le notifiche partano (in locale `MAIL_MAILER=log`)
- 2026-07-21 · 5.1.2/E6.1 · `(code)` · **Vista admin dei lead**. `Admin\Leads` (`/admin/leads`, "Richieste demo" in navbar admin, protetta `role:super-admin`): lista paginata delle richieste dal form landing, con ricerca (nome/email/attività), filtro per stato con conteggi per stato, avanzamento stato inline (nuovo→contattato→convertito/archiviato via `setStatus`, whitelistato) ed eliminazione (con conferma). Email cliccabile (`mailto:`) per rispondere al lead. I lead non sono tenant-owned → nessun TenantScope: la protezione è la rotta admin. Base operativa per l'outreach (E6.1). 8 test (224 totali)
- 2026-07-21 · 5.1.4 · `(code)` · **Cookie banner GDPR**. Componente `<x-cookie-banner />` incluso su landing, layout guest, layout legale e layout app. Poiché il sito usa **esclusivamente cookie tecnici** (sessione + Sanctum), esenti da consenso preventivo ex art. 122 Codice Privacy, il banner è **informativo** (presa visione, non consent wall): informa dell'uso dei soli cookie tecnici, linka a `/privacy#cookie` e si chiude con "Ho capito". La presa visione è persistita in **localStorage** (`replisa-cookie-consent-v1`), non in un cookie — nessun paradosso "un cookie per ricordare i cookie" — con fallback silenzioso se localStorage è bloccato. Il componente è **autonomo** (CSS + JS vanilla inline, niente Tailwind/Alpine) così rende identico anche sul layout legale, che non carica lo stesso CSS/JS. Privacy §8 ampliata con dettaglio cookie tecnici + anchor `#cookie`. Predisposto per evolvere in accept/reject granulare quando si aggiungeranno cookie di misurazione/terze parti. 6 test (230 totali)
- 2026-07-21 · 5.1.3 · `(code)` · **SEO structured data (JSON-LD) + meta completati** (chiude E5.1.3 e l'intera E5.1). Head della landing arricchito: `<link rel="canonical">`, `og:url`/`og:image`/`og:site_name`/`og:locale`, Twitter card (summary). Blocco `<script type="application/ld+json">` con `@graph` di tre nodi: **Organization** (Replisa, logo, founder Giovanni Melfi, email da `services.contact.notify_email`, sameAs giovannimelfi.com), **WebSite** (publisher → Organization) e **SoftwareApplication** (categoria BusinessApplication, descrizione, `AggregateOffer` con le 4 offerte dei piani). I prezzi delle offerte e low/high price vengono da `config/plans.php` (single source: se cambi i prezzi, il markup resta allineato). `json_encode` con `JSON_UNESCAPED_SLASHES|JSON_UNESCAPED_UNICODE`. 4 test che validano il JSON, i tipi schema.org e la coerenza offerte↔config (234 totali)
- 2026-07-22 · 4.2.2 · `(code)` · **Dettaglio conversazione nei Contatti** (chiude E4.2.2). Cliccando una riga in `/contacts` si apre un pannello laterale (slide-over) con la cronologia dei messaggi scambiati col contatto, resi come bolle chat: inbound a sinistra, outbound a destra (verdi) con timestamp e stato di consegna. Cronologia in ordine cronologico (dal più vecchio), limitata agli ultimi 200 messaggi, in sola lettura. Nuovo `Message::displayText()` che normalizza in testo leggibile i vari formati di `content` (testo/template/interattivo outbound, reply bottone/quick-reply inbound), riusato anche nel Log messaggi (E4.2.5) al posto della catena `?? ?? ??` inline. L'apertura è scoped dal TenantScope: `showConversation()` su un id di un altro tenant non trova nulla e lascia il pannello chiuso. 5 test (239 totali)
- 2026-07-22 · 4.2.1 · `(code)` · **Grafico andamento + costo Meta stimato in Dashboard** (chiude E4.2.1). Due nuovi riquadri sotto le card numeriche: (1) **Andamento messaggi** ultimi 14 giorni — grafico a barre inviati/ricevuti per giorno, aggregato in DB e con i giorni vuoti riempiti lato PHP (ogni giorno presente anche a zero), reso con barre CSS senza librerie esterne (coerente con lo stack, niente bundle JS di charting) + tooltip hover e stato vuoto; (2) **Costo Meta stimato** del mese — i template inviati nel mese (l'evento che Meta fattura: ogni template apre una conversazione business-initiated) × tariffa stimata da `config/whatsapp.php` (nuovo: tariffe per categoria Italia marketing/utility/auth/service + `estimate_rate` prudenziale marketing). Card con disclaimer chiaro che è una stima orientativa e la fatturazione reale la emette Meta. `Dashboard::TREND_DAYS` costante. 3 test (242 totali)
- 2026-07-22 · 4.2.5 · `(code)` · **Filtri data e tipo nel Log messaggi** (chiude E4.2.5). `/messages` guadagna il filtro **tipo** (testo/template/interattivo, dalle costanti `Message::TYPE_*`) e il filtro **intervallo date** (Dal/al, estremi inclusivi: `from`→startOfDay, `to`→endOfDay così i messaggi di oggi non vengono esclusi). Parsing date **difensivo** (`Carbon::createFromFormat` in try/catch): un valore non valido arrivato dall'URL viene ignorato senza errori invece di far esplodere la query. Pulsante "Azzera filtri" (visibile solo se almeno un filtro è attivo) via `resetFilters()`. Tutti i filtri restano sincronizzati nell'URL (`#[Url]`), quindi una vista filtrata è condivisibile/segnalibile. Gli input date hanno min/max incrociati (`from` non oltre `to` e viceversa). 5 test (247 totali)
- 2026-07-22 · 5.2.3 · `(docs)` · **Script demo live 5 minuti** (`docs/DEMO-SCRIPT.md`). Sceneggiatura minuto-per-minuto per la demo a un titolare di PMI: checklist di preparazione (tenant/numero/contatti/appuntamenti pronti), aggancio sul problema personalizzato per settore (ristorante/studio/palestra/commercialista), i 3 flussi in ordine **Benvenuto → Promemoria & Scadenze → Campagne** (allineato al prodotto attuale, non più il vecchio "Welcome → Reminder → Recensione"), add-on Recensioni come bonus, tour dashboard (card + grafico + costo stimato + conversazione contatto), chiusura con CTA prova 14 giorni, tabella "piano B" se un messaggio non arriva/template non approvato, e "cosa NON fare". Pronto per E6.1.4 (demo al primo interessato)
- 2026-07-22 · 5.2.2 · `(docs)` · **Template di outreach** (`docs/OUTREACH-TEMPLATES.md`). Messaggi pronti per il primo contatto a freddo verso PMI (obiettivo: fissare una demo di 15 min): email a freddo (3 oggetti + corpo <90 parole) + 2 follow-up con regola di stop, DM Instagram + follow-up soft, messaggio WhatsApp (con nota sui limiti del primo contatto fuori finestra 24h e cautela GDPR), tabella ganci per segmento (ristorante/studio/palestra/commercialista/assicurazione/parrucchiere) mappati ai flussi Replisa, principi di scrittura (corto, personalizzato, una CTA), e workflow di tracking lead che rimanda a `/admin/leads` e a `DEMO-SCRIPT.md`. Firma Giovanni Melfi / replisa.com
- 2026-07-22 · 5.2.1 · `(brand)` · **Brochure commerciale** (`brand/brochure.html`). Volantino di 2 pagine A4 pronto per la stampa (Stampa → Salva come PDF, margini "Nessuno"): HTML autonomo con CSS inline e **logo brand incorporato in base64** (nessun file esterno → inviabile per email così com'è), `print-color-adjust: exact` per i colori pieni in stampa, anteprima a schermo con ombre pagina. Pagina 1: hero, i 3 flussi (Benvenuto/Promemoria & Scadenze/Campagne), 6 benefici (canale ufficiale Meta, GDPR, personalizzabile, dashboard, setup rapido, API) e callout sui costi di conversazione Meta. Pagina 2: i 4 piani (mensile + annuale −20%) con contatti/messaggi campagna corretti da `config/plans.php`, add-on Recensioni €19, CTA demo + contatti, nota trial 14gg e costi Meta a parte. Aggiorna la versione precedente ai 4 piani attuali 19/49/99/199
- 2026-07-22 · 6.1.1 · `(docs)` · **Griglia PMI target da compilare** (`docs/PMI-TARGET.md` + `docs/pmi-target.csv`). Struttura per costruire la lista di 20-30 attività da contattare a Vittoria + provincia: colonne (attività, settore, referente, contatti, canale, priorità, gancio, stato, ultimo contatto, prossima azione), criterio di priorità Alta/Media/Bassa, **stati allineati a quelli di `/admin/leads`** (Da contattare→Contattato→Demo fissata→Cliente/Archiviato) così outreach a freddo e lead dal form vivono nello stesso imbuto, righe pre-raggruppate per settore (ristoranti/studi/palestre/commercialisti/assicurazioni/parrucchieri) con 2 esempi, idee su dove trovare i nomi (Google Maps/Instagram/giro personale) e obiettivo numerico (10 contatti → 2-3 demo → 1 cliente). Il CSV gemello è pronto per un foglio di calcolo. Resta da compilare col territorio (conoscenza dell'utente)
- 2026-07-22 · go-live · `(docs)` · **Runbook di go-live** (`docs/GO-LIVE.md`). Punto della situazione pre-lancio: stato git (main==develop, 13/26 commit non pushati), migration da eseguire in prod, variabili `.env` overlay (SMTP, Stripe key+9 Price ID, CONTACT_NOTIFY_EMAIL, APP_LOCALE), checklist infra VPS (worker/cron/build/webhook Meta), blocchi esterni (prodotti Stripe, template Meta, revisione legale) e sequenza minima consigliata per il primo cliente. Consolida in un posto i requisiti sparsi tra deploy/README, .env.example e backlog
- 2026-07-22 · go-live · `(script)` · **Script setup Stripe** (`scripts/stripe-setup.sh`). Crea via Stripe CLI i 5 prodotti (Starter/Base/Pro/Business + add-on Recensioni) e i 9 prezzi (mensili + annuali, importi da `config/plans.php`: 1900/4900/9900/19900c mese, 18200/47000/95000/191000c anno, add-on 1900c mese; `tax_behavior=exclusive` perché IVA esclusa; `lookup_key` per ritrovabilità) e stampa il blocco `.env` con i 9 Price ID pronto da incollare nell'overlay. Default in **modalità test** (sicura); `--live` per la produzione con conferma esplicita. Prerequisiti stripe CLI + jq. Non idempotente (nota nello header). Le chiavi API restano da copiare a mano
- 2026-07-22 · go-live · `(docs)` · **Runbook submission template Meta** (`docs/META-TEMPLATES-SUBMISSION.md`). Guida operativa click-by-click per creare e far approvare i 2 template obbligatori su WhatsApp Manager: prerequisiti (WABA, ruolo, nota per-WABA), passi per `appointment_reminder` (UTILITY) e `review_request` (MARKETING) con categoria/nome/lingua esatti, blocchi body da copiare, sample values, setup bottoni (quick-reply Confermo/Disdico; URL statico/dinamico per la recensione con dove trovare il link g.page), tabella cause di rifiuto comuni + rimedi, verifica post-approvazione (smoke test via app o script) e nota sul ripetere per ogni cliente. Complementare alla spec canonica `META-TEMPLATES.md` (cross-link aggiunto in entrambe le direzioni + da GO-LIVE.md)
- 2026-07-22 · 6.2.1 · `(docs)` · **Checklist onboarding primo cliente** (`docs/ONBOARDING-CHECKLIST.md`). Processo step-by-step dal "sì" del cliente ai flussi attivi e testati, in 6 fasi: (1) crea account — censimento da admin (`/admin/tenants` → invito email + licenza offline) o registrazione self-service (`/register` + trial 14gg), con nota sul settore obbligatorio; (2) raccogli dati Meta del cliente (phone_number_id/waba_id/access_token, ADR-003 per-WABA, nota verifica business se manca la WABA); (3) collega il numero in `/whatsapp` + Verifica connessione; (4) template nella WABA del cliente (rimanda a `META-TEMPLATES-SUBMISSION.md`); (5) configura i flussi (`/automations`, `/scadenze`, `/appointments` con import CSV); (6) test end-to-end (benvenuto + reminder + conferma) e formazione. Include troubleshooting inline, prerequisiti infra (worker+cron) e una checklist rapida copia-incolla. Cross-link a DEMO-SCRIPT / META-TEMPLATES-SUBMISSION / GO-LIVE / PMI-TARGET
- 2026-07-22 · 3.4.3 · `(code)` · **Segmentazione contatti (etichette)** (chiude E3.4.3). Nuovo modello `Tag` (per-tenant, `BelongsToTenant`) + pivot `contact_tag` (migrazione `tags` + `contact_tag`); `Contact::tags()` belongsToMany. **Gestione tag** nel dettaglio contatto (`Contacts`): nel pannello conversazione si aggiungono (con datalist di suggerimenti, `firstOrCreate` scoped al tenant → nessun duplicato) e si rimuovono etichette; colonna Etichette nella lista + **filtro per etichetta**. **Campagne segmentate**: `campaigns.tag_id` (nullable FK, `nullOnDelete` per non perdere lo storico) — null = tutti gli opt-in, valorizzato = solo gli opt-in con quell'etichetta. Compositore con select "Segmento" e **conteggio destinatari live**; validazione `Rule::exists` scoped al tenant (un tag altrui è rifiutato). Query destinatari centralizzata in `Contact::scopeCampaignRecipients(?tagId)`, usata **sia dal compositore (conteggio) sia da `SendCampaign` (invio)** così contano gli stessi contatti. Colonna Segmento nello storico. 9 test (256 totali)
- 2026-07-22 · 3.3.3 · `(code)` · **Tracking click recensioni via short-link** (chiude E3.3.3). I click sul button URL di un template Meta non passano dal webhook: per contarli, nuova **modalità "tracciato"** del link recensione. Modello `ReviewClick` (token univoco → URL destinazione + conteggi/timestamp) + rotta **pubblica** `GET /r/{token}` (`ReviewRedirectController`) che registra il click (conteggio + first/last clicked_at) e reindirizza (302) all'URL recensioni Google del tenant. In `ReviewRequest::request()` la scelta del parametro button è centralizzata (`buttonParam()`): tracciato → genera token + crea ReviewClick legato ad appuntamento/contatto; dinamico → suffisso fisso; statico → nessun param. UI `/automations`: terza opzione modalità + campo URL destinazione (validato `url`) + riquadro statistiche (richieste inviate / hanno cliccato / click totali). La rotta è pubblica → TenantScope no-op, lookup per token unico (pattern esistente). Migrazione `review_clicks`. `META-TEMPLATES.md §2` aggiornato con la modalità tracciata (base bottone `{APP_URL}/r/{{1}}`). 8 test (264 totali)
- 2026-07-22 · pricing/ui · `(code)` · **Prezzo pieno tagliato sull'annuale**. Nella vista annuale dei prezzi mostrato il prezzo pieno (mensile × 12) barrato accanto allo scontato −20%, per rendere visibile il risparmio: landing (`welcome.blade.php`, `€{{ prezzo*12 }}` line-through), pagina `/billing` (`billing.blade.php`, `price*12`) e brochure (`brand/brochure.html`, es. €228→€182, €588→€470, €1.188→€950, €2.388→€1.910)
- 2026-07-22 · release · `(push)` · **Push su origin di tutto il lavoro accumulato**. Pushati `main` e `develop` su `origin` (allineati, 0 commit di scarto): l'intero corpo di lavoro delle sessioni di luglio (E4.2.3 configurazione flussi → E4.3 API v1 → landing+lead+cookie+SEO → dashboard completa → E3.4.3 segmentazione → E3.3.3 tracking recensioni → prezzo annuale tagliato) è ora sul remoto. ⚠️ **Push ≠ deploy**: replisa.com **non** è ancora aggiornato — il deploy applicativo (`dploy deploy main` + migration + config + infra) resta da fare, vedi `docs/GO-LIVE.md`
- 2026-07-28 · 4.2.6 · `(fix)` · **Fix checkout Stripe in contesto Livewire + isolamento test**. Durante il primo test reale del billing (account Stripe creato, prodotti/prezzi in sandbox, 9 Price ID nel `.env`) emerso che `Billing::subscribe()`/`manageBilling()` chiamavano `Checkout::redirect()` / `redirectToBillingPortal()` di Cashier, che in una richiesta **Livewire** restituiscono un `Livewire\...\Redirector` e violano il return-type `RedirectResponse` (**TypeError**, checkout rotto). Corretto estraendo l'URL della sessione (`$checkout->url` via `Checkout::__get`) e `billingPortalUrl()`, e usando il `$this->redirect()` di Livewire (gestisce anche gli URL esterni); metodi ora `void`. Verificato end-to-end: checkout → webhook `customer.subscription.created` → `[200]` → subscription attiva in DB, `PlanLimits` risolve il piano dal prezzo e Cashier azzera `trial_ends_at` (banner prova via). NB operativa: in locale `php artisan serve` è single-thread e può perdere la POST webhook concorrente col redirect di ritorno — usare `PHP_CLI_SERVER_WORKERS>1`; in prod (php-fpm) non si pone. Inoltre `phpunit.xml` ora azzera tutte le `STRIPE_*` (chiavi + 9 Price ID): i test non dipendono più dal `.env` locale né rischiano di colpire l'API Stripe reale. 264 test verdi
- 2026-07-28 · 4.2.6 · `(ops)` · **Setup Stripe (test) + billing verificato e2e su prod**. Account Stripe creato; 4 prodotti piano + add-on Recensioni e i 9 prezzi (mensili+annuali, importi allineati a `config/plans.php`) creati in **sandbox/test**; 9 Price ID nel `.env` locale e nell'overlay di prod. Endpoint webhook di prod creato via CLI (`we_1Ty7LC` → `https://replisa.com/stripe/webhook`, gli eventi di Cashier) — **nota:** `cashier:webhook` NON va lanciato sul VPS (l'overlay non espone il secret al comando → "No API key"); l'endpoint si crea da locale/dashboard e il signing secret va nell'overlay. Il `.env` di prod è un **file copiato al deploy** (non symlink): l'overlay si applica solo con `dploy deploy main`, non col solo `config:clear`. **Billing in modalità test verificato dal vivo su replisa.com**: checkout con carta test → catena eventi completa consegnata (`customer.subscription.created` con `pending_webhooks=0`) → subscription sincronizzata (banner prova via) → Billing Portal ok. Subscription di test poi cancellate. Resta il passaggio a **LIVE** (attivazione account/P.IVA, IVA `exclusive` per Stripe Tax → prezzi da ricreare, Price ID+chiavi+webhook live). Dettaglio in memoria `stripe-billing-status` e `docs/GO-LIVE.md`
- 2026-07-29 · go-live · `(script)` · **`scripts/stripe-setup.sh` allineato al setup reale** (sblocca il passaggio a Stripe LIVE). Tre correzioni emerse dal setup in test: (1) **`tax_behavior=exclusive` → `unspecified`** — l'esercente è in **regime forfettario**, nessuna IVA da esporre: niente Stripe Tax, il prezzo mostrato è quello incassato, e i prezzi live nascono coerenti con quelli già creati a mano in test (la fattura elettronica con dicitura forfettario resta fuori da Stripe, SdI); (2) **dipendenza `jq` resa opzionale** — helper `json_id()` che usa `jq` se presente, altrimenti **`python3`** (su questa macchina `jq` non è installato → il check dei prerequisiti faceva fallire lo script prima ancora di partire); (3) **istruzione finale corretta**: l'overlay `~/.dploy/overlays/.env` si applica solo con **`dploy deploy main`**, non col solo `config:clear`, perché il `.env` di prod è un file copiato al deploy (non symlink). Importi riverificati contro `config/plans.php` (19/49/99/199 mensili, 182/470/950/1910 annuali, add-on 19). `bash -n` pulito; 264 test verdi (script non coperto da test, non tocca il codice applicativo)
- 2026-07-29 · go-live · `(code)` · **Comando `replisa:stripe-check`** — verifica dei Price ID prima del passaggio a LIVE. Copiando i prodotti in live mode dal dashboard Stripe i **Price ID cambiano**: se il `.env` resta con quelli di test il checkout fallisce con *"No such price"* al primo cliente reale, e te ne accorgi dal cliente. Il comando interroga Stripe e confronta i 9 prezzi attesi (4 piani × mensile/annuale + add-on Recensioni, derivati da `config/plans.php`) con quelli configurati, controllando per ciascuno: esistenza, **modalità live/test**, importo in centesimi, intervallo di fatturazione, valuta (vs `cashier.currency`), prezzo attivo e prodotto non archiviato. Segnala esplicitamente le configurazioni **miste** live+test (sintomo di un `.env` aggiornato a metà) e il caso "prezzo TEST con chiave LIVE". La modalità è dedotta dal prefisso di `STRIPE_SECRET`; **nessuna chiave o segreto viene mai stampato** (test dedicato che lo verifica). Exit code non-zero se qualcosa non torna → usabile in un check di deploy. Verificato dal vivo sull'attuale configurazione di test: 9/9 prezzi coerenti. 3 test sui percorsi deterministici — secret assente, Price ID mancante, deduzione modalità — senza chiamate di rete (267 totali). `docs/GO-LIVE.md` aggiornato col richiamo nel passo 1 della sequenza
- 2026-07-29 · go-live · `(ops)` · **Stripe passato a LIVE in produzione + `APP_DEBUG` spento**. Account Stripe attivato, prodotti copiati da test a live dal dashboard (che genera **Price ID nuovi**) e i 9 valori sostituiti nell'overlay `~/.dploy/overlays/.env` insieme alle chiavi `pk_live`/`sk_live`; endpoint webhook ricreato in **live mode** (il `whsec` è per-endpoint e per-modalità: quello di test `we_1Ty7LC` resta, innocuo). Push + `dploy deploy main`. **Verificato sul VPS con `replisa:stripe-check`**: *"Chiave Stripe in uso: LIVE"* e `ok (LIVE)` su tutte e 9 le voci — importo, intervallo, valuta, prezzo e prodotto attivi coerenti con `config/plans.php`, nessuna configurazione mista. Nella stessa sessione, emerso da una pagina d'errore su replisa.com che la produzione girava con **`APP_DEBUG=true`** (stack trace, versioni PHP/Laravel, percorsi `vendor/`, header esposti pubblicamente): corretto a `false` nell'overlay + `APP_ENV=production`, verificato dall'esterno che `GET /stripe/webhook` ora restituisce una 405 generica di 1 KB senza alcun indizio di debug. `POST` senza firma → `403` (validazione attiva). ⚠️ **Restano da provare**: *Send test event* con esito `200` (è l'unico modo di confermare che il `whsec` dell'overlay sia quello dell'endpoint live — il 403 prova solo che il controllo è acceso) e un **checkout reale** con carta vera + rimborso, oltre alla verifica dell'**IBAN di payout**
- 2026-07-29 · go-live · `(ops)` · **Webhook Stripe live verificato (test 1 superato)**. In modalità live il dashboard **non permette di inviare eventi fittizi** (è una funzione della sola modalità test) e sull'account live non c'era ancora alcun evento da rimandare: la verifica è stata fatta generando un **evento reale a costo zero** — creato un cliente dal dashboard, modificato (→ `customer.updated`, che è fra gli 8 eventi sottoscritti da Cashier) e poi rimosso. Esito: **`200 OK`** su `https://replisa.com/stripe/webhook` per `evt_1TybLk…` → il `STRIPE_WEBHOOK_SECRET` dell'overlay è effettivamente quello dell'endpoint live, firma validata end-to-end. Confermato per differenza che l'account **live è distinto dalla Sandbox** usata in sviluppo (`acct_1TxmXBCB9xUCpLQZ`, a cui è autenticata la Stripe CLI locale — da qui il live è irraggiungibile: `your live mode API key needs to be re-configured`). ⚠️ Il `200` certifica **solo la firma**: `WebhookController::handleWebhook()` cade su `missingMethod()` (Response vuota, 200) per ogni evento senza handler, quindi non dice nulla sulla sincronizzazione delle subscription — resta da fare il **checkout reale** con rimborso
